{%if error is defined %}
    Nie ma takiego przedmiotu w bazie danych!
{%else %}
    {% if success is defined %}
        Success!
    {% endif %}
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}
{% endif %}
